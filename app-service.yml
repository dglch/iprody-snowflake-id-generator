apiVersion: v1
kind: Service
metadata:
  name: snowflake-id-cluster-service
spec:
  selector:
    app: snowflake-id
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
